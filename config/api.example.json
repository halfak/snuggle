/* 
This configuration file specifies the behavior of the web interface for Snuggle.
*/
{
	"server": {
		"host": "0.0.0.0",
		"port": 8080,
	},
	"database": {
		"host": "localhost",
		"database": "newbies"
	},
	"mediawiki": {
		"api_url": "https://en.wikipedia.org/w/api.php"
	},
	/* The location from which Snuggle is running. */
	"root_directory": ""
}
